// SPDX-License-Identifier: MIT
pragma solidity ^0.8.30;

contract Explicita{

    function converte() public pure {

        /*uint8[4] memory array = [uint8(1),2,3,4]; 
        Precisamos converte o primeiro elemento para ele saber que, queremos os números em uint8;*/
    }

        function converte2() public pure {
            
            //address endereco = payable address(this);
            //payable(endereco).transfer;

    }
}

/*
    CONVERSÃO EXPLÍCITA

    address(this);
    payable([address]);
    uint[] memory arrray = [uint(1),...];
*/